# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'remembering' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'SwiftLint'
  pod 'SwiftFormat/CLI'

  # Pods for remembering

  target 'rememberingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'rememberingUITests' do
    # Pods for testing
  end

end
